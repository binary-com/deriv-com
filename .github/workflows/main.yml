name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Verify organization membership
        run: echo "Verifying organization membership..."

      # Rest of the workflow steps...
