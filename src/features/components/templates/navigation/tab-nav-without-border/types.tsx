import { TString } from 'types/generics'

export type OptionNavigationType = {
    src?: string
    selected_src?: string
    active_path?: string
    button_text: TString
    option_name: string
    to: `/${string}`
}
