import { TString } from 'types/generics'

export type TCalculator = {
    index: number
    name: TString
    text: TString
    image_name: string
    image_alt_name: TString
    button_text: TString
    link: string
}
