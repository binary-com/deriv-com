import React from 'react'
import { selling_points } from './data'
import TradePlatformNumbers from 'features/components/templates/trade-platform/numbers'

const CTraderNumbers = () => {
    return <TradePlatformNumbers selling_points={selling_points} />
}

export default CTraderNumbers
